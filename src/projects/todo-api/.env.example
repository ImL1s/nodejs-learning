# Environment
NODE_ENV=development
PORT=3000
HOST=127.0.0.1

# Security - ⚠️ 請生成強密鑰！
# 生成方式：node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=<GENERATE-64-CHAR-HEX>

# CORS - 允許的來源
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100

# ⚠️ 安全警告：
# 1. 絕不要在生產環境使用默認值
# 2. 絕不要將 .env 文件提交到 Git
# 3. JWT_SECRET 必須至少 64 個字符
# 4. 定期更換密鑰
